# CareerMatch Agent · AI 协作说明

> 读我 30 秒，你就知道这个项目在做什么、做到哪了、怎么配合秦俪萍。

---

## 1. 项目背景

- **项目名称**：CareerMatch Agent
- **一句话定位**：AI Agent 求职助手——输入 JD + 简历，输出匹配度分析、求职信、面试题预测
- **目标用户**：应届生求职者（核心用户：秦俪萍本人）
- **业务价值**：这是秦俪萍求职作品集的核心项目，**21 天打磨上线**，用于证明「AI PM 能独立交付 AI 产品」

---

## 2. 当前进展

- **创建日期**：2026-05-22
- **当前阶段**：工作区搭建完成（Day 1）
- **下一步**：完成 PRD v0（Day 2）
- **进度追踪**：详见 `docs/ITERATION_LOG.md`

---

## 3. 目录用途

| 目录 | 放什么 | **不放什么** |
|------|--------|-------------|
| `docs/` | 产品思考沉淀：PRD、迭代记录、决策记录、评估报告 | 代码、临时草稿、随手笔记 |
| `assets/` | 视觉素材：`design/` UI 参考、`bug/` 报错截图、`reference/` 灵感图 | 文档、数据 |
| `notes/` | 学习沉淀：`pitfalls.md` 踩坑记录、`techniques.md` 技术笔记、`insights.md` AI 协作心得 | 产品文档、代码 |
| `src/` | 所有 Python 代码。`parsers/` 解析器、`tools/` 工具函数、`agent/` LLM 核心逻辑 | 测试数据、输出文件 |
| `data/` | 测试数据：`sample_jds/` 真实 JD 样本、`sample_resumes/` 简历样本 | 代码、文档 |
| `outputs/` | 所有运行产物（匹配报告、评估结果） | **不入 git**（已在 .gitignore 屏蔽） |
| `tests/` | 测试代码 | 业务代码 |

---

## 4. 工具栈

- **语言**：Python 3.11+
- **AI 模型**：DeepSeek V4 Pro（OpenAI 兼容 API）
- **关键依赖**：`openai` · `pydantic` · `pypdf` · `python-dotenv` · `loguru`
- **后期引入**：`streamlit`（Web UI）
- **开发工具**：VS Code + Claude Code
- **部署目标**：Streamlit Cloud（后期）

---

## 5. 工作方法（最重要）

秦俪萍采用 **「构建循环 4 步走」** 开发节奏：

```
① 文档先行  →  在 docs/ 里把想法变成 PRD（先文档后代码）
② Demo 验证  →  在 src/ 写最少代码跑通核心流程，数据放 data/（最小成本验证）
③ 补全计划  →  拆解开发步骤，记录到 docs/（ITERATION_LOG.md + DECISIONS.md）
④ 开发测试  →  补全 src/ 逻辑 + 写 tests/ 测试，遇到坑写入 notes/pitfalls.md
```

### 对你的要求

- 当秦俪萍说"做 X"时，**先判断这是第几步**，不要直接丢 100 行代码
- 如果需求不明确，**主动反问**，帮她澄清"你到底想要什么效果"
- 第①步（文档阶段）→ 帮她捋清思路、写 `docs/` 下的文档，不动 `src/` 代码
- 第②步（Demo 阶段）→ 在 `src/` 用最少代码跑通，测试数据放 `data/`，产物放 `outputs/`
- 第③步（计划阶段）→ 帮她拆任务、排优先级，更新 `docs/ITERATION_LOG.md`
- 第④步（开发阶段）→ 补全 `src/` + 写 `tests/`，边写边解释，踩坑记入 `notes/pitfalls.md`

---

## 6. 关于秦俪萍

- **身份**：AI PM 应届生，不是程序员
- **能力画像**：懂产品逻辑、懂 AI 概念，但**不熟悉具体语法和工程细节**
- **协作偏好**：
  - 代码写**详细中文注释**（她需要看懂每段代码在干什么）
  - 做技术选型时，**用她听得懂的方式**解释 trade-off
  - 不要假设她知道某个库/概念——**主动用 1-2 句话解释**
  - 她喜欢"先看效果再抠细节"

---

## 7. 不要做的事

- 不要硬编码 API Key（永远从 `.env` 读）
- 不要写超过 100 行的单个函数（拆小，降低阅读门槛）
- 不要只丢代码不说话——**先解释思路，再给代码**
- 不要假设她懂某个库的用法——第一次出现时加注释说明
- 不要在她说"差不多就这样"时继续优化——那意味着停止
- 不要往 `outputs/` 提交任何文件（`.gitkeep` 除外）
